// Global demo/placeholder contact info shared across every scenario. Any
// player-facing phone number that exists purely for the simulation (order
// contact info, fake member profile, registration form...) must use this
// constant rather than a one-off hardcoded string, so it only needs to be
// right in one place. This is NOT the 165 anti-fraud hotline - that number
// is real and must never be replaced by this constant.
export const DEMO_PHONE_NUMBER = '0995-165165';
